#!/bin/sh

FILES=$(git diff --cached --name-only | grep -E '(^|/)\.env$|(^|/)\.env\.local$|(^|/)\.env\.production$')
if [ -n "$FILES" ]; then
  echo "ERROR: refusing to commit env file(s): $FILES"
  exit 1
fi
